name: "Docker hub auth"
description: "authenticates to docker hub"
inputs:
  USERNAME:
    required: true
  ACCESSTOKEN:
    required: true
runs:
  using: "composite"
  steps:
    - run: echo "${{ inputs.USERNAME }}" | sudo docker login -u "${{ inputs.ACCESSTOKEN }}" --password-stdin
      shell: bash
