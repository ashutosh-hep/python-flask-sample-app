name: "Docker hub auth"
description: "authenticates to docker hub"
runs:
  using: "composite"
  steps:
    - run: echo "${{ secrets.user }}" | sudo docker login -u "${{ secrets.accesstoken }}" --password-stdin
