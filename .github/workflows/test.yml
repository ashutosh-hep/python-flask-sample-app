name: CI

on:
  push:
    branches: [ git-test3 ]
  pull_request:
    branches: [ git-test3 ]

jobs:
  build:
    runs-on: self-hosted 

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Test
      run: echo "test hai"

